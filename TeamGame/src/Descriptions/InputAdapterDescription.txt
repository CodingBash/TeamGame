InputAdapter Class Description

InputAdapter is a manager that sets specific Mouse and Key events to other classes
such as the players. This usually is sent to classes that implements Controllable 
since it inherits a method called inputChange(boolean[] keys) which the
InputAdapter class is where the "boolea[] keys" are created. 
